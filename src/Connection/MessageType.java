package Connection;

//типи повідомлень додати
public enum MessageType {
    REQUEST_NAME_USER,
    USER_NAME,
    NAME_USED,
    NAME_ACCEPTED,
    TEXT_MESSAGE,
    USER_ADDED,
    USER_REMOVED,
    DISABLE_USER;
}
